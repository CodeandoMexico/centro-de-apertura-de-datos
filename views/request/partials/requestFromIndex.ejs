<div class="container-fluid">
  <div class="requested-data row">
    <div class="col-md-1 centered-text vote">
      <% if (req.session.user) { %>
        <%if (user_votes.indexOf(request.id) < 0) { // Logged in and hasn't voted for this one %>
          <a title="Dar mi voto" class="btn-vote btn-give-vote-unvoted" onclick="$('#voteForm').attr('action', '/user/giveVote');$('#voteFormRequestIdInput').val('<%= request.id %>');$('#voteForm').submit()">
            <i class="glyphicon glyphicon-chevron-up"></i>
          </a>
        <% } else { // Logged in and has already voted for this request %>
          <a title="Ya has votado" class="btn-vote btn-give-vote-voted">
            <i class="glyphicon glyphicon-chevron-up"></i>
          </a>
          <a title="Quitar mi voto" class="btn-vote-small btn-remove-vote" onclick="$('#voteForm').attr('action', '/user/removeVote');$('#voteFormRequestIdInput').val('<%= request.id %>');$('#voteForm').submit()">
            <i class="glyphicon glyphicon-remove"></i>
          </a>
        <% } %>
      <% } else { // Not logged in. %>
          <a title="Dar mi voto" class="btn-vote btn-give-vote-unvoted">
            <i class="glyphicon glyphicon-chevron-up"></i>
          </a>
      <% } %>
      <div class="vote-count">
        <% if (request.voted.length == 1) { %>
          1 voto
        <% } else { %>
          <%= request.voted.length %> votos
        <% } %>
      </div>
    </div>
    <div class="requested-data-info col-md-10">
      <h3 class="title">
        <a href="#" data-target="#requestInfoModal" data-toggle="modal" onclick="$('#info-modal-request-title').html('<%= request.title %>');$('#info-modal-request-description').html('<%= request.description %>');"><%= request.title %></a>
      </h3>
      <div class="request-details">
        Solicitado <%= sails.config.moment.moment(request.createdAt).fromNow() %>
        <br />
        <a href="/request/view/<%= request.id %>">Liga <i class="glyphicon glyphicon-link"></i></a>
      </div>
    </div>
  </div>
</div>
